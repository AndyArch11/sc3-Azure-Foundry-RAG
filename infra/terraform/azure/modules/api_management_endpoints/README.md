# Module: api_management_endpoints

Creates APIM API definitions separately from the APIM service itself.

Use this module to model MCP-facing APIs and their operations independently from service deployment.